extends layout

block content
  .w3-modal(style='display:block;')
    .w3-modal-content.w3-card-4
      header.w3-container.w3-teal.w3-card-4.w3-center
        h3 #{film.title}
      .w3-container.w3-margin.w3-padding.w3-section
        table.w3-table.w3-table-all.w3-margin-bottom
          tr
            th
              b Title
          tr
            td= film.title
          tr
            th
              b Year
          tr
            td= film.year

          each actor, i in film.cast
            tr
              th
                b Actor ##{i}
            tr 
              td= actor

          each genre, i in film.genres
            tr
              th
                b Genre ##{i}
            tr 
              td= genre
          
        a(href='/filmes')
          button.w3-right.w3-btn.w3-blue-gray Voltar 